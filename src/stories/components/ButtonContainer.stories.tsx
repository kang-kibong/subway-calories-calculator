import ButtonContainer from '@components/Picker/ButtonContainer'

export default {
  title: 'Component/ButtonContainer',
  component: ButtonContainer,
}

export const Defualt = (args: any) => {
  return <ButtonContainer {...args} />
}
