import styled from "@emotion/styled";

const BaseLayout = () => {
  return <LayoutContainer>asdf</LayoutContainer>;
};

export default BaseLayout;

const LayoutContainer = styled.div`
  width: 375px;
  height: 100vh;
  background-color: #fff;
`;
